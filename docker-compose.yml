services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: public
    ports:
      - "6010:5432"
    volumes:
      - parking_spots_data:/var/lib/postgresql

volumes:
  parking_spots_data:
